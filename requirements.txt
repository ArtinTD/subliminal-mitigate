# Install with:
#   pip install -r requirements.txt
#
# Targets CUDA 13.0 + PyTorch 2.9.0.
# torch 2.9.0 is pinned to match vllm==0.11.2 (last version with manylinux1 wheels,
# compatible with glibc 2.28 on Hyak/klone).
--extra-index-url https://download.pytorch.org/whl/cu129

torch==2.9.0

transformers==4.57.6
datasets==4.6.1
peft==0.18.1
trl==0.29.0
accelerate==1.13.0

unsloth==2026.3.4
vllm==0.11.2

openai>=1.87.0,<2.0.0

pyyaml>=6.0,<7.0
tqdm>=4.66.0,<5.0.0
langdetect>=1.0.9
